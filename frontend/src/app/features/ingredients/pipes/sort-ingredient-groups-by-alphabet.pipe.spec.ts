import { SortIngredientGroupsByAlphabetPipe } from './sort-ingredient-groups-by-alphabet.pipe';

describe('SortIngredientGroupsByAlphabetPipe', () => {
  it('create an instance', () => {
    const pipe = new SortIngredientGroupsByAlphabetPipe();
    expect(pipe).toBeTruthy();
  });
});
