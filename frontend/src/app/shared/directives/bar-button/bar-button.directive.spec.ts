import { StandloneBarButtonDirective } from './bar-button.directive';

describe('StandloneBarButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new StandloneBarButtonDirective();
    expect(directive).toBeTruthy();
  });
});
