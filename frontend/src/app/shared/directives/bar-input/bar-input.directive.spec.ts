import { StandloneBarInputDirective } from './bar-input.directive';

describe('StandloneBarInputDirective', () => {
  it('should create an instance', () => {
    const directive = new StandloneBarInputDirective();
    expect(directive).toBeTruthy();
  });
});
